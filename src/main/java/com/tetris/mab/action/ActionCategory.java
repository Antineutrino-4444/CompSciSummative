package com.tetris.mab.action;

/** Broad category used for grouping, ambiguity checking and UI. */
public enum ActionCategory {
    LAUNCH,
    DEFENSE,
    ANALYSIS,
    FEINT,
    UTILITY,
    RESTRAINT,
    CUSTOM
}
